{
  "name": "@oat/cdk-reference",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AWS CDK reference implementation, for comparison with the canonical Terraform. Manages nothing.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "synth": "cdk synth",
    "diff": "cdk diff"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "aws-cdk": "^2.170.0"
  }
}
